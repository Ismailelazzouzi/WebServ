# Webserv Configuration

server {
    listen 8080;
    root ./site1;
    index index.html;
    autoindex on;
}

server {
    listen 8081;
    root ./site2;
    index home.html;
    autoindex on;
}

# Optional: more server blocks to test parsing
server {
    listen 9090;
    root ./admin;
    index dashboard.html;
    autoindex off;
}
